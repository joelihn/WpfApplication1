<UserControl x:Class="WpfApplication1.CustomUI.PatientGroupPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1024" d:DesignWidth="140">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ComboBox Name="ComboBoxPatientGroup" SelectionChanged="ComboBoxPatientGroup_OnSelectionChanged" Initialized="ComboBoxPatientGroup_OnInitialized" Style="{DynamicResource ComboBoxStyle4}"/>
            <ListBox Grid.Row="1" BorderBrush="{x:Null}"/>
        </Grid>
        
    </Grid>
</UserControl>
